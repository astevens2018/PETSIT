<div class="container new_form">
  <h1>Book now!</h1>
  <%= simple_form_for [current_user.id, @host] do |f| %>
  raise
    <%= f.input :name %>
    <%= f.input :pet_type %>
    <%= f.input :date %>

    <%= f.submit "Book now!", class: "btn btn-primary" %>
  <% end %>
</div>